import { FlexWidthDirective } from './flex-width.directive';

describe('FlexWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new FlexWidthDirective();
    expect(directive).toBeTruthy();
  });
});
